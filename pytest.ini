[pytest]
testpaths = backend/tests
pythonpath = .
markers =
    postgres: integration tests that require a real PostgreSQL server (see docs/03_validation/POSTGRES_INTEGRATION.md)

# The default run is the fast SQLite suite. `pytest -m postgres` overrides this
# because command-line arguments are appended after addopts.
addopts = -m "not postgres"
